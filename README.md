A login system with Http Basic as the authentication method.

Front end: React
- Contains one screen with register and login options.
- All validations happens in Back end, except the password field that is a React component.

Back end:
- Rest API.
- Authentication with Spring Security using Http Basic.
- Validations with Bean Validation.
